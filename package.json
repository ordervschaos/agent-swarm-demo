{
  "name": "chapter-6-genome",
  "private": true,
  "type": "module",
  "scripts": {
    "start":    "node --no-deprecation --import tsx/esm index.ts",
    "watch":    "node --no-deprecation --import tsx/esm watch.ts",
    "schedule": "node --no-deprecation --import tsx/esm schedule.ts",
    "parallel": "node --no-deprecation --import tsx/esm parallel-demo.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "openai": "^4.80.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
